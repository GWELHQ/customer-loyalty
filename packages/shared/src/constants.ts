export const API_VERSION_PREFIX = '/api/v1';

/** Reconciliation thresholds: loyalty sales as a % of an attendant's total sales for the day. */
export const RECONCILIATION_NEAR_LIMIT_PCT = 80;
export const RECONCILIATION_EXCEEDED_PCT = 100;

export const PIN_MAX_FAILED_ATTEMPTS = 5;
export const PIN_LOCKOUT_MINUTES = 15;

export const DEFAULT_TIMEZONE = 'Africa/Nairobi';
